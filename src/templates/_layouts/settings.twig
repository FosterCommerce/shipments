{% extends "_layouts/cp" %}

{# Shared layout for the plugin's settings-area pages. Each settings page extends this
   and sets `selectedItem` to one of 'general' / 'emails' / 'integrations' / 'shipment-fields'
   so the sidebar highlights the current section. Mirrors the pattern in
   `vendor/craftcms/commerce/src/templates/_layouts/settings.twig`. #}

{% set selectedSubnavItem = 'settings' %}

{% set settingsNavItems = {
	'general': { title: 'settings.nav.general'|t('shipments'), url: 'shipments/settings/general' },
	'emails': { title: 'nav.emails'|t('shipments'), url: 'shipments/settings/emails' },
	'integrations': { title: 'nav.integrations'|t('shipments'), url: 'shipments/settings/integrations' },
	'shipment-fields': { title: 'settings.nav.shipmentFields'|t('shipments'), url: 'shipments/settings/shipment-fields' },
} %}

{% set selectedItem = selectedItem ?? 'general' %}

{% block sidebar %}
	<nav>
		<ul>
			{% for handle, item in settingsNavItems %}
				<li>
					<a href="{{ url(item.url) }}"{% if handle == selectedItem %} class="sel"{% endif %}>{{ item.title }}</a>
				</li>
			{% endfor %}
		</ul>
	</nav>
{% endblock %}
