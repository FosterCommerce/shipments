{% extends "shipments/_layouts/settings" %}
{% import "_includes/forms" as forms %}

{% set selectedItem = 'shipment-fields' %}
{% set title = 'settings.nav.shipmentFields'|t('shipments') %}
{% set readOnly = not craft.app.config.general.allowAdminChanges %}
{% set fullPageForm = not readOnly %}

{% if readOnly %}
	{% set contentNotice = readOnlyNotice() %}
{% endif %}

{% set crumbs = [
	{ label: 'Settings'|t('app'), url: url('settings') },
	{ label: 'Plugins'|t('app'), url: url('settings/plugins') },
	{ label: 'nav.shipments'|t('shipments'), url: url('shipments/settings/general') },
] %}

{% block content %}
	{% if not readOnly %}
		{{ actionInput('shipments/shipment-fields/save') }}
	{% endif %}

	<p>{{ 'settings.fields.intro'|t('shipments') }}</p>

	{{ forms.fieldLayoutDesignerField({
		fieldLayout: fieldLayout,
		disabled: readOnly,
	}) }}
{% endblock %}
